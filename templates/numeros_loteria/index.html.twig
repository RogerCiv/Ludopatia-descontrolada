{% extends 'base.html.twig' %}

{% block title %}NumerosLoteria index{% endblock %}

{% block body %}
    <h1>NumerosLoteria index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Numero</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for numeros_loterium in numeros_loterias %}
            <tr>
                <td>{{ numeros_loterium.id }}</td>
                <td>{{ numeros_loterium.Numero }}</td>
                <td>
                    <a href="{{ path('app_numeros_loteria_show', {'id': numeros_loterium.id}) }}">show</a>
                    <a href="{{ path('app_numeros_loteria_edit', {'id': numeros_loterium.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="3">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('app_numeros_loteria_new') }}">Create new</a>
{% endblock %}
