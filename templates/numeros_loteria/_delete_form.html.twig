<form method="post" action="{{ path('app_numeros_loteria_delete', {'id': numeros_loterium.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ numeros_loterium.id) }}">
    <button class="btn">Delete</button>
</form>
