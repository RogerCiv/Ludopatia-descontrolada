{% extends 'base.html.twig' %}

{% block title %}Apuesta{% endblock %}

{% block body %}
    <h1>Apuesta</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ apuestum.id }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_apuesta_index') }}">back to list</a>

    <a href="{{ path('app_apuesta_edit', {'id': apuestum.id}) }}">edit</a>

    {{ include('apuesta/_delete_form.html.twig') }}
{% endblock %}
